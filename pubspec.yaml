name: nitv_flutter_test
description: A test app for flutter developer.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  freezed_annotation: ^1.1.0
  injectable: ^1.5.3
  get_it: ^7.2.0
  dio: ^4.0.4
  connectivity_plus: ^2.2.1
  logger: ^1.1.0
  flutter_bloc: ^8.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner:
  injectable_generator: ^1.5.3
  freezed: ^1.1.1


flutter:


  uses-material-design: true